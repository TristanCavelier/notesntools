(function (root, factory) {
  if (typeof define === "function" && define.amd) {
    define(["exports", "thirdparty"], factory);
    return;
  }
  factory(root, thirdparty);
}(this, function (browser, thirdparty) {

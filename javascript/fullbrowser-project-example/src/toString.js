// PUT EVERY THING YOU WANT BEFORE "use strict";

/*jslint indent: 2, node: true */

"use strict";

var emptyString = "";

function toString(value) {
  if (arguments.length === 0) {
    return "";
  }
  return emptyString + value;
}
